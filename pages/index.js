export default function Home() {
  return (
    <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:'4rem'}}>
      <div style={{maxWidth:820}}>
        <h1 style={{marginBottom:12}}>Fighting Fucking Fitness — v0</h1>
        <p>
          Deployed on Vercel. This is the starter. Next step:
          we’ll render articles from JSON/Markdown pushed by Make/Airtable.
        </p>
      </div>
    </main>
  );
}
